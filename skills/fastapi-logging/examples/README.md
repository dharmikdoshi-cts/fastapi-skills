# examples — fastapi-logging

Reserved for runnable examples artifacts that complement `SKILL.md`.

Add files here when:
- a pattern needs a copy-paste-ready implementation
- a behavior needs a concrete pytest demonstration

Empty for now — populated as the skill matures.
